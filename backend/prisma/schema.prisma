generator client {
  provider = "prisma-client"
  output   = "../src/generated/prisma"
}

datasource db {
  provider = "sqlite"
}

model MonkeytypeResult {
  id        String @id
  timestamp BigInt
  wpm       Float
  accuracy  Float
}
